# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Convenções deste projeto

Ver `../AGENTS.md` e `../specs/README.md` (pasta `programacao-mobile/`).

Regra da disciplina: todo componente vive em `components/<Nome>/` com `index.js` (componente) e
`styles.js` (estilos via `StyleSheet.create`, export default). Não usar estilo inline.
